// ---------------------------------------------------------------------------
// Mock Data Index — re-exports all fixtures for easy importing
// ---------------------------------------------------------------------------
// Usage: import { mockUser, mockBookings, ... } from "@/mocks"
//
// These fixtures provide realistic placeholder data for the dashboard,
// auth flow previews, and component development. Replace with real
// Firestore/Wix data once the backend is wired up.
// ---------------------------------------------------------------------------

export { mockUser, mockUserProfile } from "./user"
export { mockBookings, mockBookingStats } from "./bookings"
export { mockSavedDiamonds } from "./saved-diamonds"
export { mockChatRooms, mockChatMessages, mockRoomMessages, mockClientNames } from "./chat"
export { mockTours } from "./tours"
export { mockActivity } from "./activity"
export { homepageContent } from "./homepage"
